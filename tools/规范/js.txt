1.使用Tab缩进；
2.尽量不要使用原生对象扩展；
3.命名使用字母、数字、下划线：
  类       驼峰式 ModuleClass()
  公有方法 混合式 getPosition()
  公有变量 混合式 frameStyle
  常量     大写式 DEFAULT_FRAME_LAYOUT

4.所有语句结束后，必须使用 ; 号结束；
5.尽量做有意义的代码折行，不要让一行代码过长；
6.重复变量建议使用 "i", "j", "k" （依次类推）等名称的变量(全世界公认)；
7.注释：
  //行注释
  /*
	块注释
  */